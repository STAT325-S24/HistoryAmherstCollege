library(tidyverse)
history_text <- tibble(x = 1, y = 2)
usethis::use_data(history_text, replace = TRUE)



history_anno <- tibble(x = 1, y = 2) # this is a stub: needs to be replaced with appropriate 
# calls to cleanNLP
usethis::use_data(history_anno, replace = TRUE)
